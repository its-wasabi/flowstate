[package]
name = "desktop"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { workspace = true }


egui = "0.29"
eframe = { version = "0.29", default-features = false, features = [
    "default_fonts",
    "wgpu",
    "wayland",
] }
